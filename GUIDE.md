Given this currently empty nextjs app, i want you to create a very simple compound. use shadcn cards and table, and anything else you can use from shadcn components.

the app should have two cards, first one with inputs, second one table.

first card with inputs should have:
starting balance, target balance, multiplier.

starting balance is how much money you have now.
ending balance is how much money you wanna reach.
multiplier is by how much you multiply your portfolio every trade, make it as shadcn dropdown with selections "1.25" "1.5" "2" "3"

table below should show number of current trade, and current balance after this trade.

inputs should have icons like some shadcn inputs examples have, for the current and target balance should be some kind of money or dollar icon

for the multiplier some x or chart up icon, you choose.

as u see we are using hugeicons library in this shadcn preset so use from them.

i want the style of app to remain the same as now since i used shadcn preset with the colors font everything that i like.

all those settings should be saved in cache so user doesnt have to reinput everything if he refreshes page
